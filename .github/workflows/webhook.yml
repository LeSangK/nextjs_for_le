name: Notify Release

on:
  release:
    types: [published]
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send notification
        run: |
          curl -X POST --data-urlencode "payload={\"channel\": \"#timeline_raku\", \"username\": \"webhookbot\", \"text\": \"カビゴン可愛い😍 \", \"icon_emoji\": \":kabigon:\"}" ${{ secrets.SLACK_WEBHOOK_URL }}
